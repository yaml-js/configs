name: Publish
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  publish_packages:
    name: Publish Packages
    uses: yaml-js/.github/.github/workflows/jobs.initialize-ci@main
    with:
      KEY: ${{ github.sha }}
      secrets: inherit
